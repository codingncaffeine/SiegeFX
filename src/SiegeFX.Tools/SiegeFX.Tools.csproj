<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\SiegeFX.Core\SiegeFX.Core.csproj" />
    <!-- Phase 22-SC-MUSIC-FOLD — pulls in AudioEngine + MusicPlayer + NLayer
         + Silk.NET.OpenAL transitively. Replaced the SiegeFX.Runtime project
         reference that was bringing in 13MB of GLFW + OpenGL native runtimes
         the headless CLI doesn't need. -->
    <ProjectReference Include="..\SiegeFX.Audio\SiegeFX.Audio.csproj" />
  </ItemGroup>

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net11.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
    <AssemblyName>siegefx</AssemblyName>
    <RootNamespace>SiegeFX.Tools</RootNamespace>
  </PropertyGroup>

</Project>
